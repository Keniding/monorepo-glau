import { Route } from '@angular/router';
import { HeaderComponent } from '../core/components/test/header/header.component';

export const remoteRoutes: Route[] = [
  { path: '', component: HeaderComponent },
];
