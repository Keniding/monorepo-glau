import { Route } from '@angular/router';
import { LandingComponent } from './core/pages/landing.component';

export const appRoutes: Route[] = [
  {
    path: '',
    component: LandingComponent,
  }
];
